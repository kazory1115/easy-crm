APP_NAME="Easy CRM"
APP_ENV=local
APP_KEY=base64:5pfCbQeEBXp2DAyHqjIvukx34uTrKxQV1/GRNMdSA/Q=
APP_DEBUG=true
APP_URL=http://localhost
APP_TIMEZONE=Asia/Taipei

APP_LOCALE=zh_TW
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=zh_TW

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# =================================================
# Database Configuration
# =================================================
# 下面為 Docker 內部連線設定（DB_HOST=mysql）
# 若要在宿主機直連，請用 DB_PORT 對外埠
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306

# DB_HOST=127.0.0.1
# DB_PORT=3406

DB_DATABASE=easy_crm_db
DB_USERNAME=easycrm
DB_PASSWORD=easycrm_password

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=localhost

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
CACHE_PREFIX=easy_crm_cache

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="noreply@easycrm.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# =================================================
# Laravel Sanctum (API Authentication)
# =================================================
# 允許的前端網域（影響 CORS/CSRF）
SANCTUM_STATEFUL_DOMAINS=localhost,localhost:5173,localhost:3000,127.0.0.1,127.0.0.1:5173,127.0.0.1:3000

# 提供給前端 build 時的名稱
VITE_APP_NAME="${APP_NAME}"

# CORS 允許來源（逗號分隔）
CORS_ALLOWED_ORIGINS=http://localhost:8180,http://localhost:5173,http://localhost:3000,http://127.0.0.1:8180,http://127.0.0.1:5173,http://127.0.0.1:3000
